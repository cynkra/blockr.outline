% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slide-block.R
\name{new_slide_block}
\alias{new_slide_block}
\title{Slide block}
\usage{
new_slide_block(
  layout = "exhibit-full",
  title = "",
  subtitle = "",
  footnote = "",
  text = "",
  labels = "",
  paginate = TRUE,
  ...
)
}
\arguments{
\item{layout}{Layout id, one of \code{names(slide_layouts())}.}

\item{title, subtitle, footnote}{Slide chrome; empty strings render nothing.}

\item{text}{The Details field: one field for every layout, so the words
survive a layout switch. \verb{- } starts a bullet, \code{1. } a numbered item,
plain lines stay plain; \verb{**bold**}, \verb{*italic*}.}

\item{labels}{Compare layout only: panel headings, \code{|}-separated.}

\item{paginate}{Single-exhibit layouts: page an overflowing table over
further slides (see \code{\link[=slide]{slide()}}). Ignored by other layouts.}

\item{...}{Forwarded to \code{\link[blockr.core:new_block]{blockr.core::new_block()}}.}
}
\description{
A block that composes one PowerPoint slide: pick a layout, fill its
fields, link exhibits, and the block's output is the finished slide at
true 16:9 -- the same inch geometry the pptx download places, painted in
HTML (see \code{\link[=slide]{slide()}}). Variadic: a layout states how many exhibits it
takes, and zero-input text slides are legal.
}
