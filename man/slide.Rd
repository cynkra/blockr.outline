% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slide-object.R
\name{slide}
\alias{slide}
\title{A composed slide}
\usage{
slide(
  layout = "exhibit-full",
  title = "",
  subtitle = "",
  footnote = "",
  text = "",
  labels = "",
  exhibits = list(),
  paginate = TRUE
)
}
\arguments{
\item{layout}{Layout id, one of \code{names(slide_layouts())}.}

\item{title, subtitle, footnote}{The slide chrome. Empty strings render
nothing (and an empty subtitle hands its room to the body).}

\item{text}{The layout's Details field -- ONE field, the same content
whatever the layout, so switching layouts reshapes the words rather
than losing them. Lines starting with \verb{- } become bullets, lines
starting with \code{1. } become numbered items (renumbered in order, as
markdown does), lines without stay plain text, and a slide freely
mixes all three. Inline \verb{**bold**} and \verb{*italic*}.}

\item{labels}{Panel headings for the compare layout, separated by \code{|}
(e.g. \code{"Before | After"}). Ignored by other layouts.}

\item{exhibits}{Linked block results, in slot order. More values than the
layout has slots are not drawn; missing ones leave the slot empty.}

\item{paginate}{Single-exhibit layouts only: a table too tall for its
slot is carried over further slides by blockr.viz's paginator (repeated
header, the title marked \verb{(2 of 3)}, the slide's subtitle and footnote
stamped on every page), exactly like the deck's paged tables. \code{FALSE}
truncates with a visible marker instead. Layouts with more than one
content slot always truncate: a second slide would duplicate the other
slots.}
}
\value{
A \code{blockr_slide} object.
}
\description{
The value a slide block evaluates to: a layout id, the authored strings
(title, subtitle, footnote and the layout's own text field) and the linked
exhibit values, in slot order. The object is inert data -- painting it is
the painters' job (the HTML preview and the officer pptx writer consume
the same \code{\link[=slide_slots]{slide_slots()}} geometry), which is what keeps the block's
preview and its download the same slide.
}
\details{
\code{text} is the chosen layout's one authored field; what it means depends on
the layout (the takeaway line under an exhibit, the bullet list of a text
slide, the kicker of a section divider). It survives a layout switch on
purpose: a takeaway rewritten as bullets is the same words in a different
shape.
}
